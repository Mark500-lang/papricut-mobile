If you do not have nodeJs install it first

Run; npm install

To view in browser emulator
Run: ionic serve

To build
ionic capacitor build ios

If you do any changes in code
npx cap copy
npx cap sync
ionic capacitor build ios
